<div class="container mt-4">
    <div class="card shadow-sm">
        <div class="card-body">

            <h2 class="card-title text-warning mb-3">
                Scoped Service
            </h2>

            <p>
                <strong>Service Instance 1:</strong>
                <span class="text-success">@ViewBag.Id1</span>
            </p>

            <p>
                <strong>Service Instance 2:</strong>
                <span class="text-success">@ViewBag.Id2</span>
            </p>

            <div class="alert alert-warning mt-3" role="alert">
                <strong>Note:</strong> Scoped services are created once per request. Therefore, the two instances above will have the same ID within the same request, but different IDs across different requests.
            </div>
        </div>
    </div>
</div>